using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Swap : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
       /* int x = 4;
        int y = 5;
        Swap(x, y);*/
    }
    (int,int) SwapValues(int a, int b)
    {
        return (b, a);
    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
